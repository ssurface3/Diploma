from setuptools import setup, find_packages

setup(
    name='YieldCurve',
    version='0.1',
    packages=find_packages(),
    description='A simple package to compute yield curves',
    author='Deez nuts',
)


